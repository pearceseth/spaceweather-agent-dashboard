/**
 * @space-weather/agent - Claude tool-use agentic layer
 */
import { VERSION as TYPES_VERSION } from "@space-weather/types"

export const VERSION = "0.0.1" as const
export const typesVersion = TYPES_VERSION
